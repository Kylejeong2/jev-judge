# TypeSafe API key (https://typesafe.ai). Never commit the real value.
TYPESAFE_API_KEY=
# Jev model: "jev-latest" (default) or a pinned version like "jev-1.13.0"
JEV_MODEL=jev-latest
# Override only for proxies; default https://api.typesafe.ai
TYPESAFE_BASE_URL=
